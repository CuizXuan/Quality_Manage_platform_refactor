# API 接口文档

> 共 126 个接口，13 个模块

## ai

共 13 个接口

### `GET` /api/ai/config

Get Ai Config

**响应**

- `200`: Successful Response

### `PUT` /api/ai/config

Update Ai Config

**请求体**

```json
{
  "$ref": "#/components/schemas/AIConfigUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/config/test

Test Ai Connection

**响应**

- `200`: Successful Response

### `GET` /api/ai/templates

List Templates

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| template_type | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/templates

Create Template

**请求体**

```json
{
  "$ref": "#/components/schemas/AIPromptTemplateCreate"
}
```

**响应**

- `201`: Successful Response
- `422`: Validation Error

### `PUT` /api/ai/templates/{template_id}

Update Template

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| template_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/AIPromptTemplateUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/ai/templates/{template_id}

Delete Template

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| template_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/generate-variants

Generate Variants

**请求体**

```json
{
  "$ref": "#/components/schemas/GenerateVariantsRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/generate-assertions

Generate Assertions

**请求体**

```json
{
  "$ref": "#/components/schemas/GenerateAssertionsRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/analyze-failure

Analyze Failure

**请求体**

```json
{
  "$ref": "#/components/schemas/AnalyzeFailureRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/summarize-report

Summarize Report

**请求体**

```json
{
  "$ref": "#/components/schemas/SummarizeReportRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/ai/suggestions/{suggestion_id}/accept

Accept Suggestion

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| suggestion_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/AcceptSuggestionRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/ai/analysis/{analysis_id}

Get Analysis

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| analysis_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

## docgen

共 22 个接口

### `GET` /api/docgen/tasks

List Tasks

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| task_type | query | string | 否 |  |
| status | query | string | 否 |  |
| keyword | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/tasks/{task_id}

Get Task

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| task_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/tasks/download/{task_id}

Download Task File

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| task_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/rules

List Rules

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| doc_type | query | string | 否 |  |
| enabled | query | string | 否 |  |
| keyword | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/rules

Create Rule

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenRuleCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/rules/{rule_id}

Get Rule

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| rule_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/docgen/rules/{rule_id}

Update Rule

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| rule_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenRuleUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/docgen/rules/{rule_id}

Delete Rule

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| rule_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/templates

List Templates

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/templates/upload

Upload Template

**请求体**

```json
{
  "$ref": "#/components/schemas/Body_upload_template_api_docgen_templates_upload_post"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/uploads/upload

Upload File

**请求体**

```json
{
  "$ref": "#/components/schemas/Body_upload_file_api_docgen_uploads_upload_post"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/docgen/uploads

List Uploads

**响应**

- `200`: Successful Response

### `DELETE` /api/docgen/templates/{template_id}

Delete Template

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| template_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/requirement/preview

Preview Requirement

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenRequirementGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/requirement/generate

Generate Requirement

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenRequirementGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/database/preview

Preview Database

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenDatabaseGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/database/generate

Generate Database

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenDatabaseGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/api/preview

Preview Api

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenApiGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/api/generate

Generate Api

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenApiGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/requirement/generate-async

Generate Requirement Async

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenRequirementGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/database/generate-async

Generate Database Async

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenDatabaseGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/docgen/api/generate-async

Generate Api Async

**请求体**

```json
{
  "$ref": "#/components/schemas/DocGenApiGenerateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

## logs

共 1 个接口

### `GET` /api/logs

List Logs

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| module | query | string | 否 |  |
| action | query | string | 否 |  |
| start_date | query | string | 否 |  |
| end_date | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

## reports

共 20 个接口

### `GET` /api/reports

List Reports

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| report_type | query | string | 否 |  |
| environment | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports

Create Report

**请求体**

```json
{
  "$ref": "#/components/schemas/ReportCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/{report_id}

Get Report

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| report_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/reports/{report_id}

Update Report

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| report_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/ReportUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/reports/{report_id}

Delete Report

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| report_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/{report_id}/quality-gates/evaluate

Evaluate Quality Gates From Report

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| report_id | path | integer | 是 |  |
| gate_type | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/defects

List Defects

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| status | query | string | 否 |  |
| severity | query | string | 否 |  |
| priority | query | string | 否 |  |
| defect_type | query | string | 否 |  |
| assigned_to | query | string | 否 |  |
| project_id | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/defects

Create Defect

**请求体**

```json
{
  "$ref": "#/components/schemas/DefectCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/defects/{defect_id}

Get Defect

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| defect_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/reports/defects/{defect_id}

Update Defect

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| defect_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/DefectUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/reports/defects/{defect_id}

Delete Defect

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| defect_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/defects/{defect_id}/transition

Transition Defect

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| defect_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/DefectStatusTransition"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/defects/stats/summary

Get Defect Statistics

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| project_id | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/quality-gates

List Quality Gates

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| gate_type | query | string | 否 |  |
| enabled | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/quality-gates

Create Quality Gate

**请求体**

```json
{
  "$ref": "#/components/schemas/QualityGateCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/reports/quality-gates/{gate_id}

Get Quality Gate

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| gate_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/reports/quality-gates/{gate_id}

Update Quality Gate

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| gate_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/QualityGateUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/reports/quality-gates/{gate_id}

Delete Quality Gate

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| gate_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/quality-gates/{gate_id}/evaluate

Evaluate Quality Gate

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| gate_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/QualityGateEvaluateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/reports/quality-gates/evaluate-all

Evaluate All Quality Gates

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| gate_type | query | string | 否 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/QualityGateEvaluateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

## scenario

共 12 个接口

### `GET` /api/scenario/runs

List Executions

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| run_type | query | string | 否 |  |
| target_id | query | string | 否 |  |
| status | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/scenario/runs/{run_id}

Get Execution

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| run_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/scenario/{scenario_id}/run

Start Execution

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |
| environment_id | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/scenario

List Scenarios

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 |  |
| status | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/scenario

Create Scenario

**请求体**

```json
{
  "$ref": "#/components/schemas/ScenarioCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/scenario/{scenario_id}

Get Scenario

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/scenario/{scenario_id}

Update Scenario

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/ScenarioUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/scenario/{scenario_id}

Delete Scenario

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/scenario/{scenario_id}/steps

Add Step

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/ScenarioStepCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/scenario/steps/{step_id}

Update Step

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| step_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/ScenarioStepUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/scenario/steps/{step_id}

Delete Step

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| step_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/scenario/{scenario_id}/steps/reorder

Reorder Steps

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| scenario_id | path | integer | 是 |  |

**请求体**

```json
{
  "type": "array",
  "items": {
    "type": "integer"
  },
  "title": "Step Ids"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

## 其他

共 2 个接口

### `GET` /api/health

Health

**响应**

- `200`: Successful Response

### `GET` /

Root

**响应**

- `200`: Successful Response

## 字典查询

共 2 个接口

### `GET` /api/dicts/{type_code}

Get Public Dict

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| type_code | path | string | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/dicts

Get All Public Dicts

**响应**

- `200`: Successful Response

## 字典管理

共 11 个接口

### `GET` /api/system/dict-types

List Dict Types

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| keyword | query | string | 否 | 搜索类型编码或名称 |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/dict-types

Create Dict Type

**请求体**

```json
{
  "$ref": "#/components/schemas/DictTypeCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/dict-types/{type_id}

Update Dict Type

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| type_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/DictTypeUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/dict-types/{type_id}

Delete Dict Type

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| type_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/system/dict-types/{type_id}/items

List Type Items

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| type_id | path | integer | 是 |  |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/system/dict-items

List Dict Items

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| type_id | query | string | 否 | 字典类型ID |
| keyword | query | string | 否 | 搜索字典编码或名称 |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/dict-items

Create Dict Item

**请求体**

```json
{
  "$ref": "#/components/schemas/DictItemCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/dict-items/{item_id}

Update Dict Item

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| item_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/DictItemUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/dict-items/{item_id}

Delete Dict Item

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| item_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/dict-items/{item_id}/toggle-status

Toggle Item Status

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| item_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/dict-items/reorder

Reorder Dict Items

**请求体**

```json
{
  "$ref": "#/components/schemas/DictItemReorderRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

## 平台系统管理

共 16 个接口

### `GET` /api/system/users

List Users

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| keyword | query | string | 否 | 搜索关键词：用户名/姓名/邮箱 |
| status | query | string | 否 | 状态筛选 |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/users

Create User

**请求体**

```json
{
  "$ref": "#/components/schemas/UserCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/users/{user_id}

Update User

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| user_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/UserUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/users/{user_id}

Delete User

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| user_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/system/roles

List Roles

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| keyword | query | string | 否 | 搜索关键词：角色名称/编码 |
| status | query | string | 否 | 状态筛选 |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/roles

Create Role

**请求体**

```json
{
  "$ref": "#/components/schemas/RoleCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/roles/{role_id}

Update Role

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| role_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/RoleUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/roles/{role_id}

Delete Role

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| role_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/system/organizations

List Organizations

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| keyword | query | string | 否 | 搜索关键词：组织名称/编码 |
| status | query | string | 否 | 状态筛选 |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/organizations

Create Organization

**请求体**

```json
{
  "$ref": "#/components/schemas/OrganizationCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/organizations/{organization_id}

Update Organization

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| organization_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/OrganizationUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/organizations/{organization_id}

Delete Organization

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| organization_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/system/menus

List Menus

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| keyword | query | string | 否 | 搜索关键词：菜单名称/编码 |
| status | query | string | 否 | 状态筛选 |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/system/menus

Create Menu

**请求体**

```json
{
  "$ref": "#/components/schemas/MenuCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/system/menus/{menu_id}

Update Menu

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| menu_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/MenuUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/system/menus/{menu_id}

Delete Menu

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| menu_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

## 平台认证

共 5 个接口

### `POST` /api/auth/login

Login

**请求体**

```json
{
  "$ref": "#/components/schemas/LoginRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/auth/register

Register

**请求体**

```json
{
  "$ref": "#/components/schemas/RegisterRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/auth/refresh

Refresh

**请求体**

```json
{
  "$ref": "#/components/schemas/RefreshRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/auth/me

Me

**响应**

- `200`: Successful Response

### `POST` /api/auth/logout

Logout

**响应**

- `200`: Successful Response

## 用例分类

共 5 个接口

### `POST` /api/case/folders

Create Folder

**请求体**

```json
{
  "$ref": "#/components/schemas/CaseFolderCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/case/folders

List Folders

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_type | query | string | 否 | 用例类型: functional 或 api |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/case/folders/{folder_id}

Get Folder

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| folder_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/case/folders/{folder_id}

Update Folder

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| folder_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/CaseFolderUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/case/folders/{folder_id}

Delete Folder

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| folder_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

## 用例管理

共 10 个接口

### `POST` /api/case

Create Testcase

**请求体**

```json
{
  "$ref": "#/components/schemas/TestCaseCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/case

List Testcases

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| case_type | query | string | 否 | 用例类型: functional 或 api |
| folder_id | query | string | 否 |  |
| folder_ids | query | string | 否 |  |
| keyword | query | string | 否 |  |
| methods | query | string | 否 |  |
| priorities | query | string | 否 |  |
| created_start | query | string | 否 |  |
| created_end | query | string | 否 |  |
| is_automated | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/case/batch

Batch Update Testcases

**请求体**

```json
{
  "$ref": "#/components/schemas/BatchUpdateRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/case/batch-delete

Batch Delete Testcases

**请求体**

```json
{
  "$ref": "#/components/schemas/BatchDeleteRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/case/{case_id}

Get Testcase

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `PUT` /api/case/{case_id}

Update Testcase

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/TestCaseUpdate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/case/{case_id}

Delete Testcase

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/case/{case_id}/copy

Copy Testcase

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/case/{case_id}/variant

Create Variant

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |

**请求体**

```json
{
  "$ref": "#/components/schemas/CaseVariantCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/case/{case_id}/variant

List Variants

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| case_id | path | integer | 是 |  |
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |
| variant_type | query | string | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

## 终端调试

共 7 个接口

### `POST` /api/terminal/debug

Debug Request

**请求体**

```json
{
  "$ref": "#/components/schemas/DebugRequestCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/terminal/internal/run

Internal Run Request

**请求体**

```json
{
  "$ref": "#/components/schemas/DebugRequestCreate"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/terminal/history

Get History

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | integer | 否 |  |
| page_size | query | integer | 否 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `GET` /api/terminal/history/{request_id}

Get Request

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| request_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `DELETE` /api/terminal/history/{request_id}

Delete Request

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| request_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/terminal/history/{request_id}/favorite

Toggle Favorite

**请求参数**

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| request_id | path | integer | 是 |  |

**响应**

- `200`: Successful Response
- `422`: Validation Error

### `POST` /api/terminal/import-document

Import Document

**请求体**

```json
{
  "$ref": "#/components/schemas/ImportDocumentRequest"
}
```

**响应**

- `200`: Successful Response
- `422`: Validation Error
